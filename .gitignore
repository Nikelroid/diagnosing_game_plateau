__pycache__/
*.py[cod]
*.egg-info/
# run logs; the committed data/probes.json is the source for every number in the paper
data/*.log
data/configs/
wandb/
paper/*.aux
paper/*.log
paper/*.out
paper/*.pdf

# Jupyter writes checkpoint copies of every file opened in the browser. They are stale
# duplicates by construction, and two of them outlived the files they copied.
.ipynb_checkpoints/

# The review workflow: the four experts, their sheets, our replies, and the generator.
# Internal work product, and the reviews are other people's text. Never published.
private/
